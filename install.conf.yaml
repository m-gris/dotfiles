---

- defaults:
    link:
      relink: true

- clean: ['~']

- link:
  #   ~/.tmux.conf: tmux.conf
  #   ~/.vim: vim
  #   ~/.vimrc: vimrc
    ~/.zshrc_course: zshrc

- create:
    - ~/Work


# - shell:
#    - [git submodule update --init --recursive, Installing submodules]
