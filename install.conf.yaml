---

- defaults:
    link:
      relink: true

- clean: ['~']

########## LINKS
# specify how files and directories should be symbolically linked
# specified as a dictionary mapping TARGET : SOURCE. (counter-intuitive)

- link:
    # reminder: the key is the TARGET and the value is the SOURCE !!!
    ~/.zshrc: zshrc
    ~/.zshrc.bak: zshrc.bak
    ~/.gitconfig: gitconfig
    ~/.config: config
    ~/.hammerspoon:
    #   ~/.tmux.conf: tmux.conf
    #   ~/.vim: vim
    #   ~/.vimrc: vimrc

- create:
    - ~/Work


- shell:
    # - [git submodule update --init --recursive, Installing submodules]
    - sleep 2
    -
      command: ./test.zsh
      stdout: true
      stderr: true
