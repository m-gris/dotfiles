---

- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.zshrc.bak: zshrc.bak
    ~/.gitconfig: gitconfig
    #   ~/.tmux.conf: tmux.conf
    #   ~/.vim: vim
    #   ~/.vimrc: vimrc

- create:
    - ~/Work


- shell:
    # - [git submodule update --init --recursive, Installing submodules]
    - sleep 2
    -
      command: ./test.zsh
      stdout: true
      stderr: true
