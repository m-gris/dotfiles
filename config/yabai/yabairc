# Default layout for yabai tiling set to binary space partioning
# other options: float or stack
yabai -m config layout bsp

# new window => to the right (if vertical split ) or bottom (horizontal split)
yabai -m config window_placement second_child

# padding
yabai -m config top_padding 12
yabai -m config bottom_padding 12
yabai -m config left_padding 12
yabai -m config right_padding 12
yabai -m config window_gap 12

# transparency / opacity 
yabai -m config window_opacity on
yabai -m config active_window_opacity 1.0
yabai -m config normal_window_opacity 0.3
#
#### mouse settings
# the mouse follows the focus on windows
yabai -m config mouse_follows_focus on 
# set alt as the mouse modifier (for the 2 configs below)
yabai -m config mouse_modifier alt 
# MOVE WINDOW left click + drag  
yabai -m config mouse_action1 move 
# RESIZE WINDOW right click + drag
yabai -m config mouse_action2 resize 
# dropping a window to the center of another, will swap
yabai -m config mouse_drop_action swap 


# "3rd party" Status / Menu Bar (Sketchybar, Übersicht...)
# add 20 padding to the top and 0 padding to the bottom of every space located on the main display
yabai -m config external_bar main:20:0
# add 20 padding to the top and bottom of all spaces regardless of the display it belongs to
yabai -m config external_bar all:20:20

# APPS to be 'ignored' by yabai
yabai -m rule --add app="^System Settings$" manage=off
yabai -m rule --add app="^Calculator$" manage=off
yabai -m rule --add app="^Karabiner-Elements$" manage=off
yabai -m rule --add app="^QuickTime Player$" manage=off

